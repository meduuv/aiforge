# Prompt validation

Treat external text as untrusted input. Validate required fields before sending requests to a model and keep system instructions separate from user-controlled content.

Do not let model output directly authorize privileged actions without an explicit application-side validation step.
